[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
    --cov=platformkit
    --cov-report=term-missing
markers =
    slow: 慢测试（需要外部服务，跳过默认）
    integration: 集成测试（需要启动 FastAPI）
