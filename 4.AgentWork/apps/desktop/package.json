{
  "name": "@aw/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "AgentWork 桌面端 - Tauri 2 + React 18",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "node -e \"import('child_process').then(({execSync}) => { try { const out = execSync('netstat -ano | findstr :1420 | findstr LISTENING', {stdio: 'pipe'}).toString(); const pids = [...new Set(out.split(/\\r?\\n/).map(l => l.trim().split(/\\s+/).pop()).filter(p => p && /\\d+/.test(p)))]; pids.forEach(p => { try { execSync(`taskkill /F /PID ${p}`, {stdio: 'ignore'}); console.log('Killed PID ' + p + ' on port 1420'); } catch {} }); } catch {} })\"",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-store": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0",
    "lucide-react": "^0.400.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.6",
    "typescript": "^5.5.0",
    "vite": "^5.3.4"
  }
}
