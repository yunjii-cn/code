[package]
name = "aw-desktop"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
description = "AgentWork 桌面端 - Tauri 2 后端"
publish = false

[lib]
name = "aw_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
# Tauri 核心
tauri = { version = "2.0", features = [] }
tauri-plugin-shell = "2.0"

# Workspace 共享依赖
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
chrono = { workspace = true }

# TimeFlow 引擎（W2 接入，先声明）
timeflow-core = { path = "../../../platformkit/crates/timeflow-core" }
# Git 兼容层（W4 接入）
aw-git = { path = "../../../platformkit/crates/aw-git" }
# AI 语义层（W5 接入）
timeflow-ai = { path = "../../../platformkit/crates/timeflow-ai" }
# 团队协作引擎（W7 接入）
agent-team = { path = "../../../platformkit/crates/agent-team" }

[features]
# 默认启用自定义协议
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
